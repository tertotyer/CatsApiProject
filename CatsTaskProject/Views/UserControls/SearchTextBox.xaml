<UserControl x:Class="CatsTaskProject.Views.UserControls.SearchTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CatsTaskProject.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="140"
             Height="30" Width="140">
    <Grid Background="White">
        <TextBox x:Name="inputTextBox" Height="30" VerticalContentAlignment="Center" 
                 FontSize="16" FontWeight="Light" Background="Transparent"
                 Padding="5 0 0 0"
                 TextChanged="inputTextBox_TextChanged"
                 GotFocus="inputTextBox_GotFocus"
                 LostFocus="inputTextBox_LostFocus"/>
        <TextBlock x:Name="placeholderTextBlock" Text="{Binding Placeholder}" 
                   FontSize="16" FontWeight="Light" Foreground="DarkGray" 
                   VerticalAlignment="Center" Margin="5 0 0 0"
                   Panel.ZIndex="-1"/>
        <Button x:Name="clearButton" Width="30" Height="30" HorizontalAlignment="Right" Content="X" Foreground="LightGray" Background="Transparent" BorderBrush="Transparent"  Click="clearButton_Click"/>
    </Grid>
</UserControl>
